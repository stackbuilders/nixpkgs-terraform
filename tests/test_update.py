from unittest.mock import patch
import unittest
import update

class UpdateTestCase(unittest.TestCase):
    @unittest.
    def test_is_stable(self):
        with unittest.mock.patch("github.GitRelease.GitRelease") as mock:
            moc
            self.assertTrue(update.is_stable(mock))
